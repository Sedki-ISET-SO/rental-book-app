<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="{{ path('index')}}">Home</a>
          </li>
        {% if is_granted('ROLE_SUPER_ADMIN') %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin')}}">admin</a>
            </li>
        {% else %}
          <li class="nav-item">
            <a class="nav-link" href="{{ path('rules')}}">Rules</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('newListing')}}">Add new listing</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="{{ path('userListings')}}">Your listings</a>
          </li>
          
          {% if app.user %}
            <li class="nav-item">
              <a class="nav-link" href="{{ path('userProfil')}}">Profile</a>
            </li>
          {% endif %}
        
        {% endif %}
      </ul>
      <form class="d-flex">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-danger" type="submit">Search</button>
      </form>
    </div>
  </div>
</nav>